---
- name: Create a user with sudo privileges
  hosts: all
  become: yes
  remote_user: cvadmin
  vars_files:
    - ansible_user_pass.yml
  vars:
    - ssh_key_file: "/home/{{ username }}/.ssh/id_rsa"
    - username: ansible
  tasks:

    - name: Ensure pip is installed
      apt:
        name: python3-pip
        state: present

    - name: Install passlib using pip
      pip:
        name: passlib
        state: present
        executable: pip3
        extra_args: --break-system-packages

    - name: Create a new user
      user:
        name: "{{ username }}"  # Replace with desired username
        password: "{{ my_password | password_hash('sha512') }}"  # Replace 'your_password' with the actual password
        shell: /bin/bash
        state: present
        
    - name: Add user to sudo group
      user:
        name: "{{ username }}"
        groups: sudo
        append: yes

    - name: Allow passwordless sudo for the user (Debian/Ubuntu)
      lineinfile:
        path: /etc/sudoers
        state: present
        regexp: '^{{ username }}'
        line: '{{ username }} ALL=(ALL) NOPASSWD:ALL'
        validate: '/usr/sbin/visudo -cf %s'

    - name: Ensure the .ssh directory exists
      file:
        path: "/home/{{ username }}/.ssh"
        state: directory
        mode: "0700"
        owner: "{{ username }}"
        group: "{{ username }}"

    - name: Add SSH public key to authorized_keys on remote hosts
      authorized_key:
        user: "{{ username }}"
        key: "{{ lookup('file', '/home/ansible/.ssh/id_rsa.pub') }}"
        state: present