- name: Deploy Home Assistant Namespace
  hosts: k8s-proxy
  become: yes

  tasks:

    - name: Create a k8s namespace
      kubernetes.core.k8s:
        name: home-assistant
        api_version: v1
        kind: Namespace
        state: present
        kubeconfig: /home/ansible/.kube/config