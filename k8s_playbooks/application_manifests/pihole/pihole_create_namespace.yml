- name: Deploy pihole Namespace
  hosts: k8s-proxy
  become: yes

  tasks:

    - name: Create a k8s namespace
      kubernetes.core.k8s:
        name: pihole
        api_version: v1
        kind: Namespace
        state: present
        kubeconfig: /home/ansible/.kube/config